%h1 Teachers::Group
%table.table
  %tr Title
- @groups.each do |group|
  %tr
    %td.info= group.title
    %td.info= link_to 'Show', teachers_group_path(group.id)
    %td.info= link_to 'Edit', edit_teachers_group_path(group.id)
    %td= link_to 'delete', teachers_group_path(group.id), |
      method: :delete,                          |
      data: { confirm: 'Are you sure?' }        |
   






link_to 'New group', new_teachers_group_path




%button.btn.btn-success{:type => "button"} New Group, new_teachers_group_path(@groups)


%p Find me in app/views/teachers/groups/index.html.erb
